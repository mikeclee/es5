/**
 * Game 
 */

// Global namespacing
var BubbleShoot = window.BubbleShoot || {};
// Game object within the namespace
// Immediately Invoked Function Expression IIFE
BubbleShoot.Game = (function($) {
  var Game = function() {};
  return Game;
})(jQuery);
